import { test } from 'node:test';

// Placeholder so `node --test` has at least one matching file under the integration glob.
// Remove when Story 1.2 (`Ref` domain primitive) lands its first real integration test.
await test('scaffold: integration test runner is wired', () => {});
